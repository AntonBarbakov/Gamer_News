name: Deploy static site to GitHub Pages

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    name: Build (PR check)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Simple validation step to ensure the expected file exists
      - name: Validate site files
        run: test -f frontend/index.html

      # Only upload files on push to main branch
      - name: Upload static files
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend

      # I will add build step later.
      # - name: Build
      #   run: |
      #     echo "Add your build steps here"

  deploy:
    name: Deploy to GitHub Pages
    # Deploy only on push to main branch
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4